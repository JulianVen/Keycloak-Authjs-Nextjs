import SignIn from "@/core/components/sign-in";

export default function LoginPage() {
  return (
    <main style={{ display: "grid", placeItems: "center", minHeight: "60vh" }}>
      <SignIn />
    </main>
  );
}
